import { describe, it, expect } from "bun:test";
// import { one, two } from "../src";

describe("should", () => {
    it("1", () => {
        expect(1).toBe(1);
    });

    it("2", () => {
        expect(2).toBe(2);
    });
});
